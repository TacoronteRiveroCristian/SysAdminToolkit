# InfluxDB Backup Service - Environment Configuration

#
# Source InfluxDB Configuration
#
# URL of the source InfluxDB server (required)
SOURCE_URL=http://source-influxdb:8086
# Comma-separated list of source databases to backup (required)
SOURCE_DBS=metrics,telegraf
# Source server authentication (if needed)
SOURCE_USER=
SOURCE_PASSWORD=
# Time period for grouping data in queries
SOURCE_GROUP_BY=5m

#
# Destination InfluxDB Configuration
#
# URL of the destination InfluxDB server (required)
DEST_URL=http://destination-influxdb:8086
# Comma-separated list of destination databases (must match SOURCE_DBS in order)
DEST_DBS=metrics_backup,telegraf_backup
# Destination server authentication (if needed)
DEST_USER=
DEST_PASSWORD=

#
# Backup Options
#
# Comma-separated list of measurements to backup (empty means all)
MEASUREMENTS=
# Client timeout in seconds
TIMEOUT_CLIENT=20
# Number of days to split data into when paginating large datasets
DAYS_OF_PAGINATION=7
# Path to log file
LOG_FILE=/var/log/backup_influxdb/backup.log
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

#
# Scheduling
#
# Cron expression for scheduled backups (e.g., "0 */6 * * *" for every 6 hours)
# Leave empty for on-demand backup only
BACKUP_SCHEDULE=

#
# Docker Network (used in docker-compose.yaml)
#
# Name of the network where InfluxDB servers are accessible
INFLUXDB_NETWORK=influxdb_network
